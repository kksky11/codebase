var PaisabazaarGuc = require('./index');

if (typeof window !== 'undefined') window.PaisabazaarGuc    = (_siteId) => {
    return new PaisabazaarGuc({
        siteId: _siteId,
        environment: "development",
    });
}
